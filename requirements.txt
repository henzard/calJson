streamlit>=1.36
pandas>=2.2
numpy>=1.26
camelot-py[base]>=1.0  # optional but recommended
pdfplumber>=0.11       # optional but recommended
pymupdf>=1.24          # optional; used for fast rendering to images
pdf2image>=1.17        # optional fallback for rendering
pytesseract>=0.3.10    # for OCR (fallback)
img2table>=1.4.0       # lightweight table detection for scanned pages
Pillow>=10.3
